# ---------- secrets / env ----------
.env
.env.*
!.env.example

# ---------- python ----------
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/

# ---------- node / vite / typescript ----------
node_modules/
dist/
.vite/
*.tsbuildinfo
# `vite.config.ts` is the source of truth — these are tsc/vite outputs
frontend-react/vite.config.d.ts
frontend-react/vite.config.js

# ---------- AWS SAM build output ----------
# Regenerated by every `sam build`; never commit (3000+ files of cruft).
.aws-sam/

# ---------- ad-hoc test artifacts ----------
dispatcher-out.json
*-out.json
.tmp-*

# ---------- editor / OS ----------
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
